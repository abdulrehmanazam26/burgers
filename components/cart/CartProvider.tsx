"use client";
import {createContext,useContext,useEffect,useMemo,useState} from "react";
import {Minus,Plus,ShoppingBag,X} from "lucide-react";
export type CartProduct={id:string;name:string;price:number}; type CartLine=CartProduct&{quantity:number};
const CartContext=createContext<{count:number;add:(item:CartProduct)=>void;open:()=>void}|null>(null);
export const useCart=()=>{const value=useContext(CartContext);if(!value)throw new Error("CartProvider missing");return value};
export function CartProvider({children}:{children:React.ReactNode}){const [items,setItems]=useState<CartLine[]>([]),[visible,setVisible]=useState(false),[submitted,setSubmitted]=useState(false);useEffect(()=>{try{setItems(JSON.parse(localStorage.getItem("brut-cart")||"[]"))}catch{}},[]);useEffect(()=>{localStorage.setItem("brut-cart",JSON.stringify(items))},[items]);const add=(item:CartProduct)=>{setItems(old=>{const found=old.find(x=>x.id===item.id);return found?old.map(x=>x.id===item.id?{...x,quantity:x.quantity+1}:x):[...old,{...item,quantity:1}]});setVisible(true);setSubmitted(false)};const change=(id:string,delta:number)=>setItems(old=>old.map(x=>x.id===id?{...x,quantity:x.quantity+delta}:x).filter(x=>x.quantity>0));const subtotal=items.reduce((sum,x)=>sum+x.price*x.quantity,0),delivery=subtotal?199:0;const value=useMemo(()=>({count:items.reduce((s,x)=>s+x.quantity,0),add,open:()=>setVisible(true)}),[items]);return <CartContext.Provider value={value}>{children}<div className={`cart-shade ${visible?"is-open":""}`} onClick={()=>setVisible(false)}/><aside className={`cart-drawer ${visible?"is-open":""}`} aria-hidden={!visible}><div className="cart-head"><div><small>YOUR ORDER</small><h2>THE BAG.</h2></div><button onClick={()=>setVisible(false)} aria-label="Close cart"><X/></button></div>{submitted?<div className="order-success"><ShoppingBag/><h3>ORDER RECEIVED</h3><p>Your COD request is ready. Live database delivery will activate after the restaurant database is connected.</p><button onClick={()=>setSubmitted(false)}>BACK TO BAG</button></div>:!items.length?<div className="empty-cart"><ShoppingBag/><h3>YOUR BAG IS EMPTY.</h3><p>Add a Zinger from the menu.</p></div>:<><div className="cart-lines">{items.map(x=><div className="cart-line" key={x.id}><div><b>{x.name}</b><span>Rs. {(x.price*x.quantity).toLocaleString("en-PK")}</span></div><div className="quantity"><button onClick={()=>change(x.id,-1)}><Minus/></button><strong>{x.quantity}</strong><button onClick={()=>change(x.id,1)}><Plus/></button></div></div>)}</div><form className="checkout" onSubmit={e=>{e.preventDefault();setSubmitted(true)}}><input required placeholder="Full name"/><input required type="tel" placeholder="Phone number"/><textarea required placeholder="Delivery address"/><div className="totals"><span>Subtotal <b>Rs. {subtotal.toLocaleString("en-PK")}</b></span><span>Delivery <b>Rs. {delivery}</b></span><strong>Total <b>Rs. {(subtotal+delivery).toLocaleString("en-PK")}</b></strong></div><button className="place-order">PLACE COD ORDER →</button><small>Cash on delivery · Pakistan</small></form></>}</aside></CartContext.Provider>}
