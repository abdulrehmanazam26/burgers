"use client";
import {useEffect,useState} from "react";
import {Menu,X} from "lucide-react";
import {site} from "@/data/site";
export function Navbar(){const [open,setOpen]=useState(false);useEffect(()=>{document.body.style.overflow=open?"hidden":"";return()=>{document.body.style.overflow=""}},[open]);const close=()=>setOpen(false);return <><header className="nav"><a href="#top" className="wordmark" aria-label="BRUT home">BRUT<span>®</span></a><nav aria-label="Primary navigation">{site.nav.map(x=><a key={x} href={`#${x.toLowerCase()}`}>{x}</a>)}</nav><a className="order" href="#order">ORDER NOW ↗</a><button className="mobile-trigger" onClick={()=>setOpen(v=>!v)} aria-label={open?"Close menu":"Open menu"} aria-expanded={open} aria-controls="mobile-navigation">{open?<X/>:<Menu/>}</button></header><nav id="mobile-navigation" aria-label="Mobile navigation" className={`mobile-menu ${open?"open":""}`} aria-hidden={!open}>{site.nav.map(x=><a onClick={close} key={x} href={`#${x.toLowerCase()}`}>{x}</a>)}<a onClick={close} href="#order">ORDER NOW ↗</a></nav></>}
